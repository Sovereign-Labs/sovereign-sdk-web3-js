{
  "name": "sovereign-sdk-web3-js",
  "private": true,
  "version": "0.0.0",
  "description": "Sovereign SDK JS monorepo",
  "scripts": {
    "ci": "pnpm run -r ci",
    "fix": "pnpm -r fix",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "pkg-version": "changeset version && pnpm install --lockfile-only",
    "release": "pnpm run build && changeset publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@changesets/cli": "^2.27.9"
  }
}
